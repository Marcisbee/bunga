import { style } from '@vanilla-extract/css';

export const activeStyle = style({});

export const actionStyleStyle = style({
  display: 'flex',
  position: 'absolute',
  pointerEvents: 'all',
  overflow: 'visible',
  flexDirection: 'column',
});
